import React from "react";
import { Container, LoadingIndicator } from "./styles";

const Loading = () => {
  return (
    <Container>
      <LoadingIndicator />
    </Container>
  );
};

export default Loading;
