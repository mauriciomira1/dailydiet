import React from "react";
import { Container, LogoIcon, LogoText } from "./styles";

const Logo = () => {
  return (
    <Container>
      <LogoIcon />
      <LogoText>Daily{"\n"}Diet</LogoText>
    </Container>
  );
};

export default Logo;
